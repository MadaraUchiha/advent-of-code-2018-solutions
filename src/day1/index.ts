import {inspect} from 'util';

inspect('This file is here to make TypeScript treat the project as modules, instead of as legacy.');
inspect('This file can safely be ignored, it is only here so that there is at least one import statement in the project.');